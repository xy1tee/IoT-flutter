name: star_trek_facts5
description: "A new Flutter project for Star Trek facts."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Додаємо залежність для HTTP запитів
  http: ^0.15.0
  # Для роботи з локальним сховищем (SharedPreferences)
  shared_preferences: ^2.0.9
  # Для роботи з Dio (заміна http на більш потужний варіант)
  dio: ^5.0.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Якщо є зображення або інші активи, їх можна додати тут
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # Можна додавати власні шрифти, якщо необхідно
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
